import { namespace } from './constants'

export default {
  requestLoading: `${namespace}/REQUEST_LOADING`,
  requestError: `${namespace}/REQUEST_ERROR`,
  requestSuccess: `${namespace}/REQUEST_SUCCESS`,
}
